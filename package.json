{
  "name": "treeditor",
  "version": "1.0.0",
  "description": "",
  "default": "index.html",
  "scripts": {
    "dev": "parcel index.html --port 8000",
    "build": "rm -rf ./dist && parcel build --no-cache index.html --dist-dir ./dist && cp -r ./resources ./dist/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "three": "^0.156.1"
  },
  "devDependencies": {
    "parcel": "latest",
    "prettier": "3.0.3"
  }
}
